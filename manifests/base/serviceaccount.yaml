apiVersion: v1
kind: ServiceAccount
metadata:
  name: workload
  labels:
    app.kubernetes.io/name: workload
  # The cloud-identity (Workload Identity) annotation is added per-overlay so each
  # agent's KSA binds to its own least-privilege GSA — no static cloud key in-cluster.
