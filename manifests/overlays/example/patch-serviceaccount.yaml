apiVersion: v1
kind: ServiceAccount
metadata:
  name: workload
  annotations:
    # Bind this KSA to a least-privilege Google service account (Workload Identity).
    # No static cloud key sits in the cluster (ADR-0002). Placeholder:
    iam.gke.io/gcp-service-account: "workload@REPLACE_PROJECT_ID.iam.gserviceaccount.com"
